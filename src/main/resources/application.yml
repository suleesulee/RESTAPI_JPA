spring:
  h2:
    console:
      enable: true
      path: /h2-console

  datasource:
    #url: jdbc:h2:mem:backofficeDb
    url: jdbc:h2:file:C:\Users\seong\OneDrive\Lecture\2. zerobase\Zerobase - Backend\jpa100\fastcampus\sample\backofficeDb
    driver-class-name: org.h2.Driver
    username: root
    password: '1111'

  jpa:
    hibernate:
      ddl-auto: create-drop
    generatie-ddl: true

    properties:
      format_sql: true
      hibernate:
        show-sql: true

logging:
  file:
    name: ./logs/spring-jpa.log


###네이버 API정보

naver-api:
  client-id: ddd
  client-secret: dsfsdf